// /** Arithmetic Operators - These are operators that help in doing mathematical operation
//  * Examples of arithmetic operators are:
//  * ~ Addition (+)
//  * ~ Subtraction (-)
//  * ~ Division (/)
//  * ~ Multiplication (*)
//  * ~ Modulus (%)
// // 

let x = 57;
let y = 21;

// let total = x+y;   // This will add the two vvariables
// let difference = x-y; // This will subtract the two variables
// let div = x/y; // This will divide the two variables
// let multi = x*y; // This wil; multiply the two variables
// let mod = x%y; // This will provide the remainder of the two variables


// Alice went to a bookstore and bought 3 books for Ksh120 each. (120*3=360)
//  She also bought 2 notebooks for Ksh30 each.(30*2=60)
// 360+60=420  5000-420=4580

// If she paid with a Ksh5000 bill, how much change did she receive?

let books = 120;
let noteb = 30;
let cash = 5000;

let tbooks = books*3;
let tnoteb = noteb*2;

let total = tbooks+tnoteb;
let balance = cash-total;
alert(balance)


/** comparison operators - Are used to compare two values
 * Examples of comparison operators are
 *  ~greater than (>)
 *  ~less than (<)
 *  ~greater than or equal to (>=)
 *  ~less than or equal to (<=)
 *  ~equal to (==)
 *  ~not equal to (!=)
 *  ~strict equal to (===)**/
 
let p = 10;
let q = 5;

alert(p>q); //greater than
alert(p<q); //less than
alert(p>=q); //greater than or equal to
alert(p<=q); //less than or equal to
alert(p==q); //equal to
alert(p!=q); //not equal to 
alert(p===q); //strict equal to

// Control statements - used to control flow of program
// They are two: Conditional statement and loop statement

// Conditional statement :
// if statement
        //  if(condition) {execution}          let a = 10
                                            //    if(a>15) {alert (a);}
// if else statement
        // if (condition) {execution} else {execution}           let a = 10 
                                                                //   if(a>15) {alert (a);}  else {alert (not true);}


// if else if statement
    // if (cond) {exec} else if (cond) {exec} else {}            let num = 100
                                                                // if (num>101) {alert (num);} else if (num == 100) {alert (num);} else {alert (no num)}

                                                                // terminate at the end of the alert

// switch statement


// Loop Statement :
// For
// While
// 
//




